//import * as types from '../../actions/ActionTypes';

const initialState = {
    pathname: '123'
};

export default function(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
